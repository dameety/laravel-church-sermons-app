<?php

use Illuminate\Database\Seeder;

class CategoryTableSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
/*        factory(App\Category::class, 4)->create()->each(function($u) {
        	$u->sermons()->save(factory(App\Sermon::class)->make());
        });*/
    }
}
